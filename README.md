# DispatchIQ

DispatchIQ supports both SQLite and PostgreSQL. Default is SQLite (zero setup). For production with multiple users, switch to PostgreSQL by setting `DB_TYPE=postgresql` in `.env` file (see `.env.example`).
